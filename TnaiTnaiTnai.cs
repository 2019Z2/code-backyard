using System;
using System.Linq;
using System.Collections.Generic;

namespace BenDover
{
    public class TnaiTnaiTnai
    {         
        int grade;
        Console.WriteLine("Enter Grade");
        grade=int.Parse(Console.ReadLine());
        if (grade>=0 && grade<=100)
        //if (grade>=55)
        //Console.WriteLine("Your grade seems legal, and bigger than 54. congrats!");
        //else 
        //Console.WriteLine("Your grade is invalid. resort to a manual check.");
        if (grade>=55)
        Console.WriteLine("Bigger yhan 55. Congrats!");
        else 
        if (grade >0);
        Console.WriteLine("Invalid: Cannot be negative");
        else
        Console.WriteLine("Invalid: Highest grade is 100.");
        //else Console.WriteLine("Smaller or equal to 55. Failure!")
        else Console.WriteLine("Invalid grade. resort to a manual check.");
        
    }
}